{
  "name": "jarvis-voice-assistant",
  "version": "1.0.0",
  "description": "JARVIS-style AI voice assistant: voice commands, automatic background web search, graceful fallbacks when no API key is available",
  "type": "module",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node --test \"test/*.test.js\""
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.111.0"
  },
  "license": "MIT"
}
